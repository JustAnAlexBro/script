
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // эта команда чтобы выводить инфу в консоль
        print("Hello World");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
